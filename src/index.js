import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// make playlist viewing page with the add and delete playlist options
// view individual movies
// make site responsive css 
// finnish up.
// make playlist movie cards look like other ones
// one more thing for movie playlists, movies are dragable
    // and have a switch in edit mode for a movie ranking mode possibly?

reportWebVitals();

